# Python
__pycache__/
native/build/
native/driver/**/x64/
native/driver/**/ARM64/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Local configuration and secrets
.env
.env.*
!.env.example
config.local.*

# Large, locally vendored Parakeet model payload. The tracked manifest records
# provenance and hashes; packaging requires these files to exist locally.
models/parakeet/*.onnx
models/parakeet/tokens.txt

# Private collaboration context; never publish these with the project.
user.md
arden-handoff.md
design-references/

# Build and editor output
build/
dist/
packaging/.cache/
packaging/.work/
packaging/out/
*.egg-info/
.vscode/
.idea/
